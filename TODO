-Add a multi part bar (see runebar).
-Add color and size changes(seperate options for each) for the font.
-Maybe add maelstrom weapon, or maybe just write a new mod for that, as it will be more CPU intensive than Combopoints, when it is going.
